#!/bin/bash
echo " > Running in the container < "
echo "|------------------------------------------$(date)-|\n"
docker exec -it php84 bash -c "$*"
echo "|----------------------------------------------|\n"
echo " > Executed in the container < "